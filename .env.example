# Keep this file as a template. Put the real key in .env.local, never in source code.
OPENAI_API_KEY=
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Server-only Supabase credentials for Phase 3 pgvector search.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
